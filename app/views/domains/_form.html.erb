
<%= simple_form_for(@domain) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name, label: "Nazwa" %>
    <%= f.association :server, label: "Serwer", input_html: { class: "select2" } %>
    <%= f.association :user, label: "Użytkownik", input_html: { class: "select2" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Zapisz", class: "btn btn-primary"%>
  </div>
<% end %>
